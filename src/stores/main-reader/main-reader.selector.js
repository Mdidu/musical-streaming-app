export const selectMainReaderRef = (state) => state.mainReader.audioRef;
export const selectIsListen = (state) => state.mainReader.isListen;
export const selectSelectedAlbum = (state) => state.mainReader.selectedAlbum;
